from setuptools import setup
import persistidor

setup(
    name='persistidor',
    version=persistidor.__version__,
    description='Modulo de definición de la persistencia de datos',
    author='VV',
    author_email='vvalotto@gmail.com',
    packages=['persistidor'],
)
